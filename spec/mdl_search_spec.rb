require 'spec_helper'

describe MdlSearch do
  it 'has a version number' do
    expect(MdlSearch::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
